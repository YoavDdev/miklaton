-- ============================================================
-- שלב ב' של הסרת סיסמאות זמניות גלויות (YOA-12, ממצא C5)
-- ⚠️ להריץ רק *אחרי* פריסת הקוד החדש (שכבר לא כותב לעמודה)
--    ואחרי שלב א' (20260817a).
--
-- DROP COLUMN מוחק גם את כל הערכים הקיימים — אין צורך ב-UPDATE נפרד.
-- אף קוד לא קורא את העמודה (אומת בסריקת הריפו).
-- ============================================================

alter table public.password_resets
  drop column if exists temp_password_plain;
