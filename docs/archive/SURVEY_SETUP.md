# מערכת סקרי שביעות רצון - הוראות התקנה

## 📋 סקירה כללית

מערכת מלאה לניהול סקרי שביעות רצון למוקד העירוני. מנהלת המוקד יכולה ליצור סקרים, לשלוח לינקים דרך WhatsApp, ולקבל תוצאות מפורטות עם סטטיסטיקות.

## 🚀 שלבי ההתקנה

### 1. הרצת סקריפט Database

יש להריץ את הסקריפט הבא ב-Supabase SQL Editor:

```bash
/Users/yoavdrai/CascadeProjects/windsurf-project/miklaton/scripts/CREATE-surveys-tables.sql
```

הסקריפט יוצר:
- טבלת `surveys` - סקרים שנוצרו על ידי מנהלת המוקד
- טבלת `survey_responses` - תשובות לסקרים (אנונימי או עם שם)
- פונקציה `generate_survey_token()` - ליצירת טוקן ייחודי לכל סקר
- RLS Policies - הרשאות גישה
- Indexes - לביצועים מיטביים

### 2. אימות שהקבצים נוצרו

ודא שהקבצים הבאים קיימים:

#### API Routes:
- ✅ `/app/api/surveys/route.js` - יצירה, קריאה ועדכון סקרים
- ✅ `/app/api/surveys/[id]/responses/route.js` - קריאת תשובות לסקר
- ✅ `/app/api/surveys/submit/route.js` - מילוי סקר (ציבורי, ללא התחברות)

#### Components:
- ✅ `/components/SurveyManager.js` - ממשק ניהול סקרים

#### Pages:
- ✅ `/app/call-center-manager/page.js` - עודכן עם טאב "סקרים"
- ✅ `/app/survey/[token]/page.js` - דף מילוי סקר ציבורי

## 📊 תכונות המערכת

### למנהלת המוקד:
1. **יצירת סקר חדש** - כפתור אחד, כותרת פשוטה
2. **שיתוף דרך WhatsApp** - הודעה מוכנה להעתקה
3. **צפייה בתוצאות** - סטטיסטיקות + תשובות מפורטות
4. **ייצוא לאקסל** - CSV עם תמיכה בעברית
5. **סגירת/פתיחת סקר** - שליטה מלאה
6. **התראות** - קבלת הודעה על כל תשובה חדשה

### למשיבים (ציבורי):
1. **ללא התחברות** - כל מי שיש לו לינק יכול למלא
2. **אנונימי או עם שם** - בחירה חופשית
3. **4 שאלות מדורגות** - סקלה 1-4 + "לא רלוונטי"
4. **שדה טקסט חופשי** - הערות לשיפור
5. **מניעת spam** - לא ניתן למלא פעמיים ביום מאותו IP
6. **ממשק נקי ופשוט** - 2-3 דקות למילוי

## 🎨 השאלות בסקר

1. **אדיבות** - נציגי המוקד מתנהגים באדיבות כלפיך
2. **מקצועיות** - נציגי המוקד מקצועיים בהעברת פרטי הפנייה
3. **נכונות לסייע** - נציגי המוקד מוכנים לסייע כאשר מתעוררת בעיה
4. **פתרון בעיות** - נציגי המוקד מתאמצים לעזור בפתרון הבעיה

## 📱 דוגמת הודעת WhatsApp

כאשר יוצרים סקר, המערכת מייצרת הודעה מוכנה:

```
היי! 👋
נשמח לשמוע את דעתך על השירות שלנו.
מלא/י סקר קצר (2-3 דקות):
https://[YOUR-DOMAIN]/survey/abc12345
תודה! 🙏
```

## 🔒 אבטחה

- **RLS Policies** - רק מנהלת מוקד רואה תוצאות
- **IP Limiting** - מניעת spam (תשובה אחת ליום לכל IP)
- **Token Based** - כל סקר עם טוקן ייחודי
- **No Delete** - לא ניתן למחוק סקרים, רק לסגור
- **Audit Trail** - תיעוד מלא של מתי נוצר, מתי נסגר

## 📈 סטטיסטיקות

המערכת מציגה:
- ממוצע ציונים לכל שאלה
- כמות תשובות עם שם vs אנונימי
- כמות הערות חופשיות
- תשובות מפורטות לכל משיב
- ציר זמן - מתי כל תשובה הגיעה

## 🔄 Flow מלא

### יצירת סקר:
1. מנהלת מוקד → טאב "סקרים" → "סקר חדש"
2. מזינה כותרת (למשל: "סקר אפריל 2026")
3. לוחצת "צור סקר"
4. מקבלת לינק ייחודי

### שליחת סקר:
1. לוחצת "WhatsApp" → ההודעה מועתקת
2. שולחת בקבוצת WhatsApp למטפלים
3. כל מטפל לוחץ על הלינק

### מילוי סקר:
1. מטפל נכנס ללינק (ללא התחברות!)
2. ממלא שם (אופציונלי)
3. עונה על 4 שאלות
4. כותב הערות (אופציונלי)
5. לוחץ "שלח"
6. מקבל הודעת תודה

### צפייה בתוצאות:
1. מנהלת מוקד רואה התראה על תשובה חדשה
2. לוחצת "תוצאות" ליד הסקר
3. רואה סטטיסטיקות מסכמות
4. רואה כל תשובה בנפרד
5. יכולה לייצא לאקסל

## 🎯 Best Practices

1. **שם הסקר** - כלול תאריך/חודש לזיהוי קל
2. **תדירות** - מומלץ פעם בחודש-חודשיים
3. **תזמון** - שלח אחרי אירוע או תקופה עמוסה
4. **מעקב** - בדוק תוצאות באופן שוטף
5. **פעולה** - השתמש בהערות לשיפור בפועל
6. **סגירה** - סגור סקר אחרי שבוע-שבועיים

## ⚠️ הערות חשובות

- **לא ניתן למחוק סקרים** - רק לסגור (ההיסטוריה חייבת להישמר!)
- **תשובות מוצפנות** - IP נשמר להגנה, לא מוצג למנהלת
- **התראות** - מנהלת מקבלת התראה על כל תשובה חדשה
- **ייצוא** - קובץ CSV תומך בעברית (UTF-8 BOM)

## 🧪 בדיקה

לאחר ההתקנה, בצע:

1. ✅ התחבר כמנהלת מוקד
2. ✅ עבור לטאב "סקרים"
3. ✅ צור סקר ראשון
4. ✅ העתק את הלינק
5. ✅ פתח בחלון incognito
6. ✅ מלא את הסקר
7. ✅ חזור לממשק מנהלת
8. ✅ בדוק שהתשובה הגיעה
9. ✅ ייצא לאקסל

## 📞 תמיכה

בעיות? בדוק:
- Tables נוצרו ב-Supabase
- RLS Policies פעילות
- Function `generate_survey_token()` קיימת
- Notifications table קיימת

---

**בהצלחה! 🎉**
